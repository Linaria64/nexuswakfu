---
import { Button } from "@/components/ui/button";
import { A, P, Span, Titleh1 } from "@/components/ui/composant";
import { Container } from "@/components/ui/container";
import Layout from "../layouts/layout.astro";
---

<Layout
  class="flex flex-col w-full rounded-none border-none min-h-screen bg-gradient-to-br from-[#333] to-[#13099e]"
>
  <Button className="border border-amber-400">je test un nouveau boutton</Button
  >

  <Button variant="glassmorphism">je test un nouveau boutton</Button>

  <!-- Fond avec effet glassmorphism -->
  <Container
    className="absolute inset-0 bg-gradient-to-br from-blue-500/20 via-purple-500/20 to-cyan-500/20 z-0"
  />
  <Container className="absolute inset-0 backdrop-blur-3xl z-0" />

  <!-- Contenu -->
  <Container
    className="glass-card p-8 md:p-12 max-w-2xl w-full relative z-10 text-center transform hover:scale-105 transition-all duration-300"
  >
    <Titleh1 className="text-6xl md:text-8xl font-bold text-white mb-6">
      <Button variant={"glassmorphism"} className="border border-amber-400"
        >404</Button
      >
      <Span className="text-cyan-400 animate-pulse inline-block">404</Span>
    </Titleh1>

    <P className="text-2xl md:text-3xl text-white/90 mb-8 font-bold">
      T'ES PERDU SALE MERDE ?!
    </P>

    <Container className="flex flex-col items-center gap-6">
      <P className="text-xl text-white/80 italic mb-8 animate-bounce">
        ↓ Clique ici et retourne d'où tu viens, Sokiro ↓
      </P>

      <A href="/"> DÉGAGE D'ICI ! </A>
    </Container>
  </Container>

  <Span>salut</Span>

  <!-- Éléments décoratifs flottants -->
  <Container
    className="absolute top-20 left-10 w-20 h-20 bg-red-500/20 rounded-full blur-xl animate-pulse"
  />
  <Container
    className="absolute bottom-20 right-10 w-32 h-32 bg-orange-500/20 rounded-full blur-xl animate-pulse delay-150"
  />
  <Container
    className="absolute top-1/2 left-1/4 w-24 h-24 bg-yellow-500/20 rounded-full blur-xl animate-pulse delay-300"
  />
</Layout>
